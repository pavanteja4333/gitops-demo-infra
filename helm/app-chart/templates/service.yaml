apiVersion: v1
kind: Service
metadata:
  name: app-service
  labels:
    app: go-app
spec:
  ports:
    - port: {{ .Values.app.service.port }}
      targetPort: 8080
      name: httpport
  selector:
    app: go-app
